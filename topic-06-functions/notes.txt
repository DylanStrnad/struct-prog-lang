Imperative langauge: make instructions for the outcome
Declaration: already has instructions for the outcome

IMPLEMENTING FUNCTIONS:
paramerization - functions with parameters.
    * includes name, parameters, block, return statement

Function call eval:
1. bind func to func obj.          ex. apply = function(x){print x}
2. create the calling envrionment. ex. apply(10)

Dynamic scoping - when create new envrionment, parent is the calliing envrionment.

EBNF:
function_expression = "function" ([parameters list]) { statement_list}
parareter_list= identifier { "," identifier}

complex_expression = factor {}

generative funciton - function that returns another function.
code:
n = 1
function g() {
    n = 12 // creates or updates only local variable
    f = function(x){
        ...
    }
}

Closure - hold onto envrionment even when its not part of global envrionment. Function has a pointer to the parent envrionment it needs to hold onto.
When function goes out of scope, envrionment is dropped.

ex.)
x3 = make_say_hi(3)

make_say_hi(n){
    char = "!"
    x.sayhi function(name){
        for i = 0 to n
        print(char)
    }
}

ex.)
make_clock(increment)
    seconds = 0
    tick()
        seconds = seconds + increment
    time()
        return seconds

clock = make_clock
clock.tick()
clock.time()


*Quiz - Evaluate based on types