*ON QUIZ* When are arguments evaluated? - before calling the function. 

*Lazy Evaluator - do not evaluate experssion until it needs to evaluate

exp_if = function(cond., f_then, f_else){
    if(and)
        reutrn f_then()
        else f_else()
}

STORING FUNCTIONS
A = function(x){
    print(x)
}

calling
a(x)

NESTED ENVIRONMENT (dynamic binding)
local_environment["$parent"] = environment

*function call is an expression